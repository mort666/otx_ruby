machine:
  ruby:
    version: 2.1.5
database:
  override:
    - echo 'no database'
dependencies:
  pre:
    - gem install brakeman
    - gem install bundler
test:
  post:
    - brakeman --exit-on-warn -o $CIRCLE_ARTIFACTS/brakeman.html
